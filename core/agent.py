# Backwards-compatible entry point for agent classes
# Extracted into separate files to split large files by class

from core.vessel_state import VesselState, VesselStateTracker
from core.dispatcher_agent import DispatcherAgent
from core.signal_processor_agent import SignalProcessorAgent
